# Deployment manifest — Streamlit Cloud installs from this file.
# Installs the lead_intel package plus all runtime + UI dependencies
# (pydantic, httpx, beautifulsoup4, openpyxl, reportlab, streamlit, pandas, ...).
# For local dev with test/lint tooling use:  pip install -e ".[dev,ui]"
.[ui]
