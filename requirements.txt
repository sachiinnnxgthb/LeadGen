# Core runtime dependencies (Phase 1 — domain + config).
# Provider / audit / export / UI dependencies are declared as optional extras in
# pyproject.toml and pinned here as they are wired up in later phases.
pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
python-dotenv>=1.0
eval-type-backport>=0.2; python_version < '3.11'
