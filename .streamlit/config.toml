# Streamlit config (applies locally and on Streamlit Cloud).
# Only the brand accent is set so Streamlit's native light/dark themes both work
# and stay switchable via the ⋮ menu → Settings → Theme.
[theme]
primaryColor = "#3A6EA5"
font = "sans serif"

[server]
headless = true

[browser]
gatherUsageStats = false
